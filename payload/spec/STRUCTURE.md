# Structure

Folders and conventions. What lives where, and the rule that decides where a new
file goes.

## Layout

```
```

## Conventions

- **Naming:**
- **Where a new module goes:**
- **What must never import what:** the dependency rule that keeps the layers
  honest. Write it down even if it feels obvious today.

## Tests

Where tests live, and how a test names the acceptance criterion it proves
(`@spec:AC-xxx` in the test title — that annotation is what the audit reads).
