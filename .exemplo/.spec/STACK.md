# Stack

Technologies and the exact commands that drive them. An agent reads this file to
learn how to build, run and test without guessing — so a command written here
wrong is worse than a command missing.

## Runtime

## Backend

## Frontend

## Data

## Infrastructure

## Commands

| What | Command |
|---|---|
| install |  |
| run (dev) |  |
| test |  |
| lint |  |
| typecheck |  |
| build |  |

Anything in this table that gets run more than once belongs in the `Makefile`
too, mirrored in `README.md`.
